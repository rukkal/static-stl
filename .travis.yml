sudo: required
dist: trusty

language: cpp

matrix:
  include:

    - compiler: gcc
      addons: &gcc5
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - gcc-5
            - g++-5
      env: COMPILER=g++-5 TOOLCHAIN_FILE=gcc.cmake BUILD_TYPE=Debug

    - compiler: gcc
      addons: *gcc5
      env: COMPILER=g++-5 TOOLCHAIN_FILE=gcc.cmake BUILD_TYPE=Release

    - compiler: clang
      addons: &clang37
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.7
          packages:
            - clang-3.7
      env: COMPILER=clang++-3.7 TOOLCHAIN_FILE=clang.cmake BUILD_TYPE=Debug

    - compiler: clang
      addons: *clang37
      env: COMPILER=clang++-3.7 TOOLCHAIN_FILE=clang.cmake BUILD_TYPE=Release

    - compiler: clang
      addons: &clang38
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.8
          packages:
            - clang-3.8
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang.cmake BUILD_TYPE=Debug

    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang.cmake BUILD_TYPE=Release

    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang-asan.cmake BUILD_TYPE=Debug

    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang-asan.cmake BUILD_TYPE=Release

    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang-msan.cmake BUILD_TYPE=Debug

    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang-msan.cmake BUILD_TYPE=Release
    
    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang-ubsan.cmake BUILD_TYPE=Debug

    - compiler: clang
      addons: *clang38
      env: COMPILER=clang++-3.8 TOOLCHAIN_FILE=clang-ubsan.cmake BUILD_TYPE=Release

before_script:
  - export CXX=$COMPILER

script:
  - cmake --version
  - mkdir build && cd build
  - cmake -DCMAKE_TOOLCHAIN_FILE=../toolchain_files/$TOOLCHAIN_FILE -DCMAKE_BUILD_TYPE=$BUILD_TYPE -G"Unix Makefiles" ..
  - make
  - ./test-sstl
  - ./test-sstl-noexceptions

